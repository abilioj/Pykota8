<?php

//ini_set('default_charset', 'UTF-8');
//json
//header("Content-Type: " . "text/plain");
//header("Content-Type: application/json");
//header("Content-Type: text/html; charset=UTF-8", true);

//require './config.php';
//echo '<br>' . ROOT . '<br>';
//echo ROOT_FILE . '<br>' . "<br/><br/><br/>";

/*
  $indicesServer = array(
  'PHP_SELF',
  'argv',
  'argc',
  'GATEWAY_INTERFACE',
  'SERVER_ADDR',
  'SERVER_NAME',
  'SERVER_SOFTWARE',
  'SERVER_PROTOCOL',
  'REQUEST_METHOD',
  'REQUEST_TIME',
  'REQUEST_TIME_FLOAT',
  'QUERY_STRING',
  'DOCUMENT_ROOT',
  'HTTP_ACCEPT',
  'HTTP_ACCEPT_CHARSET',
  'HTTP_ACCEPT_ENCODING',
  'HTTP_ACCEPT_LANGUAGE',
  'HTTP_CONNECTION',
  'HTTP_HOST',
  'HTTP_REFERER',
  'HTTP_USER_AGENT',
  'HTTPS',
  'REMOTE_ADDR',
  'REMOTE_HOST',
  'REMOTE_PORT',
  'REMOTE_USER',
  'REDIRECT_REMOTE_USER',
  'SCRIPT_FILENAME',
  'SERVER_ADMIN',
  'SERVER_PORT',
  'SERVER_SIGNATURE',
  'PATH_TRANSLATED',
  'SCRIPT_NAME',
  'REQUEST_URI',
  'PHP_AUTH_DIGEST',
  'PHP_AUTH_USER',
  'PHP_AUTH_PW',
  'AUTH_TYPE',
  'PATH_INFO',
  'ORIG_PATH_INFO') ;
  echo '<table cellpadding="10">' ;
  foreach ($indicesServer as $arg) {
  if (isset($_SERVER[$arg])) {
  echo '<tr><td>'.$arg.'</td><td>' . $_SERVER[$arg] . '</td></tr>' ;
  }
  else {
  echo '<tr><td>'.$arg.'</td><td>-</td></tr>' ;
  }
  }
  echo '</table>' ;

 */
//$conn = new Conexao();
//$sqlR = new SqlRules();
//$sqlRR = new SqlRulesRelatorios();

//$q = null;
//if(is_null($q))
//echo "é null";
//$d = new Data();
//$fuso = new DateTimeZone('America/Belem');
//$data = new DateTime();
//$data->setTimezone($fuso);
//echo $data->format('Y-m-d H:i:s');
//echo '<br/>';

//$dir = new Dir_File();
//$arrayDir = $dir->ScanDiretorio('c:/www');
//foreach ($arrayDir as $row):
//    echo $row . '<br/>';
//endforeach;

//$objAut = new ADauthUser();

phpinfo();